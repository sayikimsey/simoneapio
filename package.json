{ "name": "simone-api-project", "version": "1.0.0",
 "description": "Orchestrates the startup of all services.",
  "main": "start.js",
   "scripts": { "dev": "pm2 start ecosystem.config.js --env development",
    "start": "pm2 start ecosystem.config.js --env production" },
     "dependencies": { "concurrently": "^8.2.0" },
 "devDependencies": { "pm2": "^5.3.0" }
 }